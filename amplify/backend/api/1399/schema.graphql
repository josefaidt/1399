enum TodoStatus {
  ACTIVE
  COMPLETED
}

type Todo @model {
  name: String! @primaryKey(sortKeyFields: ["status"])
  status: TodoStatus!
}

type Todo2 @model {
  name: String! @primaryKey(sortKeyFields: ["status"])
  status: String!
}

type Todo3 @model {
  name: String! @primaryKey
}
